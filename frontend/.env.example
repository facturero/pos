# URL del backend local del POS (ver backend/.env -> PORT)
VITE_API_BASE_URL=http://127.0.0.1:4000

# Canal de tiempo real local (socket.io del backend, ver backend/.env ->
# LOCAL_SOCKET_PORT)
VITE_LOCAL_SOCKET_URL=http://127.0.0.1:4001
